# Agent memory

Working memory is the state needed by the current run. Long-term semantic memory stores selected facts across sessions and retrieves them when relevant. Message history is not automatically durable memory, and not every message should be embedded or retained.