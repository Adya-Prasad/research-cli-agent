# Embeddings

An embedding model maps text into a dense numerical vector. Semantically related passages should occupy nearby regions of vector space. Normalized vectors allow cosine similarity to be computed with a dot product. Chunking prevents long documents from being silently truncated.